## 热点刀锋每日执行（全自动模式）

执行「热点刀锋」(hotspot-blade) 技能，**全流程自动执行，无需等待用户确认**。

---

### ⚠️ 执行前必查

1. 执行 `date` 确认当天日期
2. 所有热榜数据必须是**当天发布**的，有明确时间戳
3. 排除无时间戳或时间戳为昨天的话题
4. 非交易日（周末/节假日）需在报告中注明"数据来源日期"

---

### 第一阶段：环境检查

```bash
opencli doctor
```
确认 Daemon + Extension 全绿再继续。

---

### 第一点五阶段：数据源健康检查（核心原则）

**⚠️ 核心原则：没有稳定的数据源，一切工具都是空幻。**

```bash
# 数据源健康检查
python3 -c "
import json
import subprocess

data_sources = [
    ('知乎热榜', 'opencli zhihu hot -f json --limit 5'),
    ('微博热搜', 'opencli weibo hot -f json --limit 5'),
    ('36氪热榜', 'opencli 36kr hot -f json --limit 5'),
    ('v2ex热榜', 'opencli v2ex hot -f json --limit 5'),
]

for name, cmd in data_sources:
    try:
        result = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
        if result.returncode == 0:
            try:
                data = json.loads(result.stdout)
                print(f'✅ {name}：成功获取 {len(data)} 条数据')
            except json.JSONDecodeError:
                print(f'⚠️ {name}：返回数据格式错误')
        else:
            print(f'❌ {name}：命令执行失败')
    except subprocess.TimeoutExpired:
        print(f'⏰ {name}：命令超时')
    except Exception as e:
        print(f'❌ {name}：错误 - {e}')
"
```

**故障自动切换策略：**
- 知乎热榜失败 → curl_cffi备用方案 → v2ex热榜替代
- 微博热搜失败 → 36氪热榜替代
- Buzzing.cc失败 → browser extract备用 → Hacker News替代
- 所有数据源失败 → 停止执行，发送故障报告，不使用过期数据

---

### 第二阶段：热榜抓取（四源互补）

```bash
# 知乎热榜 ✅ 必抓
opencli zhihu hot -f json

# 知乎热榜备用方案（当opencli zhihu hot失败时）
# 方案1：使用curl_cffi直接抓取知乎热榜API
python3 -c "
from curl_cffi import requests
import json
resp = requests.get('https://www.zhihu.com/api/v3/feed/topstory/hot-lists/total?limit=10', impersonate='chrome', timeout=15)
if resp.status_code == 200:
    data = resp.json()
    for item in data.get('data', []):
        target = item.get('target', {})
        title = target.get('title', '')
        print(f'  - {title}')
else:
    print(f'  ❌ 知乎热榜API返回状态码：{resp.status_code}')
"

# 微博热搜 ✅ 必抓
opencli weibo hot -f json

# Buzzing.cc 海外信息差 ✅ 强烈推荐（28个海外媒体聚合，中文翻译）
# 方法A: curl_cffi（无需浏览器，推荐定时任务使用）
python3 -c "
from curl_cffi import requests
import re
resp = requests.get('https://buzzing.cc', impersonate='chrome', timeout=15)
# 优化正则：匹配20-200字符的标题文本，排除URL和短文本
titles = re.findall(r'>([^<]{20,200})<', resp.text)
seen = set()
for t in titles:
    t = t.strip()
    if t and t not in seen and len(t) > 15 and not t.startswith('http') and not t.startswith('//'):
        seen.add(t)
        print(f'  - {t[:120]}')
        if len(seen) >= 30:
            break
"

# 科技垂直补充（36氪/HN/IT之家）✅
opencli 36kr hot -f json          # 36氪热榜
opencli hackernews top -f json    # Hacker News英文热榜
opencli ithome hot                # IT之家数码/消费电子
```

**tophub.today 已弃用** — 2026-05-07 三工具（curl_cffi/scrapling/browser）交叉验证确认：IP被Cloudflare风控标记，数字验证码自动化无法绕过。不要反复尝试。

**热点刀锋标准配置 = 知乎 + 微博 + Buzzing.cc（海外信息差）+ 36氪/HN（科技垂直）。** 四源覆盖国内大众舆论 + 海外信息差 + 科技垂直，已足够支撑每日选题。

---

### 第三阶段：候选话题筛选

从抓取结果中筛选候选话题，按以下标准评分：

| 维度 | 权重 |
|------|------|
| 热度 | 30% |
| 九边适配性 | 40%（★★★★★/★★★★ 优先） |
| 讨论空间 | 20% |
| 话题时效性 | 10% |

**排除规则：** 娱乐/饭圈/无具体矛盾/太过专业化小众话题

**多样性规则：** 同一子类话题最多选2个
- 子类分类：教育/就业/科技/消费/社会/国际/海外

**自动选择：** 按九边适配性从高到低，选前5个话题，直接进入写作，不等待确认。

---

### 第四阶段：爆款微头条全流程生成

对每个话题，依次执行以下七步，**一次性完成全部七步再输出终稿**：

#### 第一步：选题定位
- 分析话题核心矛盾
- 确定九边式靶向（怼谁？）
- 确定情绪方向（无奈/讽刺/愤怒）

#### 第二步：钩子设计
- 开场：数字暴击 + "说真的，并没有" + 悖论钩子
- 中段："说白了"定性 + 数据跟进 + "你猜？"
- 结尾："但更关键的是"递进 + 预言 + 具体追问

#### 第三步：草稿写作 + 快速预览检查
- 九边版暴风雪开场
- 九边版胖大山打脸
- 九边版递进收尾
- 快速预览检查（草稿完成后立即检查）：
  □ 核心矛盾是否清晰？（一句话能说清）
  □ 九边句式是否自然？（不生硬堆砌）
  □ 数字/细节是否足够？（≥3个具体细节）
  □ 情绪铺垫是否到位？（打脸前有铺垫）
- 如不达标，立即调整再继续下一步

#### 第四步：共鸣适配
- 替换专业词为普通人感知词
- 检查追问是否具体

#### 第五步：钩子强化检查（九边句式七条清单）
```
□ 开场：数字 + "你以为X？并没有。说真的Y。"
□ 打脸：说白了，X不是在干Y，是在干Z。
□ 定性：这不叫X，这叫Y。
□ 冷幽默：[某现象]听了这话，默默X了。
□ 递进：很明显X。但更关键的是Y。
□ 追问：你猜X？Y？还是Z？
□ 数字幽默：数字排比，不用加形容词。
```

#### 第六步：算法发布检查 + 标题A/B测试 + 算法优化
- 标题A/B测试：生成3个候选标题，按算法友好度评分
  □ 标题A：数字暴击型（如："3个真相告诉你..."）
  □ 标题B：反差对比型（如："月薪3千和月薪3万的人，区别在这"）
  □ 标题C：悬念提问型（如："为什么年轻人不愿生孩子？"）
- 标题评分标准：
  - ≤25字，有数字+反差感（+2分）
  - 无"竟然""必须""震惊"等绝对词（+1分）
  - 慎用问号（头条算法打压）（+1分）
  - 包含情绪关键词（无奈/讽刺/愤怒）（+1分）
- 选择评分最高的标题作为最终标题
- 标签：1-3个精准标签，基于话题分类推荐
- 发布时机：基于历史数据推荐最佳时段（早8-9点/午12-13点/晚20-22点）
- 算法优化三要素：
  □ 开头3秒钩子：前3秒必须抛出核心矛盾或悬念，抓住用户注意力
  □ 中间互动引导：每300字设置一个互动引导（提问/反转/数据冲击）
  □ 结尾行动号召：明确引导用户点赞/评论/转发（如："你觉得呢？""转发给需要的人"）
- 发布时机详细分析：
  □ 早高峰（8:00-9:00）：通勤时间，用户刷手机频率高，适合发布轻松/娱乐类内容
  □ 午休时间（12:00-13:00）：午休时间，用户有空闲，适合发布深度/思考类内容
  □ 晚高峰（18:00-19:00）：下班时间，用户放松，适合发布社会/生活类内容
  □ 睡前时间（20:00-22:00）：睡前刷手机，用户情绪敏感，适合发布情感/共鸣类内容
  □ 周末全天：用户时间充裕，适合发布长文/深度分析类内容
- 根据话题类型选择最佳发布时段：
  - 教育/就业类：早高峰或午休时间
  - 科技/消费类：晚高峰或睡前时间
  - 社会/国际类：睡前时间或周末全天
  - 海外信息差类：早高峰（与海外时差同步）

#### 第七步：润色加固 + 三宗罪自检（标准化指标）
```
第一宗：细节模糊，缺少具象感（细节数≥3个/篇）
□ 每个关键名词有型号/价格/场景细节
□ 无"某"字带过的模糊表述
□ 标准化检查：整篇微头条中具体细节（数字、品牌、价格、地点等）是否≥3个？

第二宗：逻辑跳脱，过渡生硬（逻辑转折≤2处/篇）
□ 每个大转折前有承上启下的过渡句
□ 情绪铺垫到位才打脸
□ 标准化检查：整篇微头条中逻辑转折点是否≤2处？超过2处需要增加过渡句

第三宗：口语碎句，表达冗余粗糙（自问自答≤3处，九边句式不堆叠）
□ 连续短句每句都有新信息
□ 自问自答不超过3处
□ 九边句式集中在不同段落（不堆叠）
□ 标准化检查：自问自答句式是否≤3处？九边句式是否分散在不同段落？
```

---

### 第五阶段：Wiki存档

每篇微头条终稿存到：
```
C:\Users\yingm\wiki\sources\market-intelligence\daily\{date}-热点刀锋微头条-5篇.md
```

格式：`2026-05-05-热点刀锋微头条-5篇.md`

---

### 第六阶段：Telegram 推送

推送**两条消息**：

**消息1：执行报告**
```
✅ 热点刀锋已完成（{日期}）
📊 抓取：知乎 + 微博 + Buzzing.cc + 36氪/HN/IT之家 + X trending
🏆 选中话题：
1. {话题1}（{热度} {来源}）
2. {话题2}（{热度} {来源}）
...
📝 5篇终稿已生成，详见 Wiki
```

**消息2：5篇微头条全文**
每篇单独一条消息，格式：
```
📌 微头条① | {话题名}
{全文}

#热点刀锋
```

---

### 质量控制原则

- **不等待用户确认**，直接用适配性最高的5个话题
- **一次性完成七步**，不拆分步骤输出
- **不过滤话题给用户**，但需过三宗罪自检
- 热榜抓取失败时记录原因，用已有数据继续执行
- 任何步骤出错，记录到 Wiki 执行笔记，继续执行后续步骤

---

### 参考
完整工作流和命令见 `hotspot-blade` skill，不在此重复粘贴。
